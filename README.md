# hello-world-ai-test

Deskflow sandbox: issues on this repo are implemented by an autonomous coding
agent with per-ticket Docker preview environments.

Run locally: `docker compose up` → http://localhost:3000
